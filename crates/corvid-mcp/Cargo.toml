[package]
name = "corvid-mcp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Sidecar MCP server exposing a corvid store to agentic tools"
# Binaries ship on engine releases, not crates.io; the engine crate
# (package `corvid-db`) is the only published member for now.
publish = false

[dependencies]
# Package `corvid-db`, lib ident `corvid` — `use corvid::…` unchanged.
corvid-db.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tempfile.workspace = true
